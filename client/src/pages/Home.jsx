import { useState, useEffect } from 'react';

const Home = () => {
    // const { data: users, isPending, error } = useFetch('/api');

    return (
        <div className="home">
            <p>Home</p>
        </div>
    );
}

export default Home;